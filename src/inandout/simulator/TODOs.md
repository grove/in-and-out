# TODOs

- generated id can be smarter (use next sequence if it looks like a sequence in the seeds)
- emulate pagination (parameters for pagination should be exposed in the swagger ui)
- wrap response in record_selector
